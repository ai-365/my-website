### WSL

WSL允许你在Powershell中使用Linux命令，也允许你在Windows上运行Linux系统环境。

查看可用的发行版列表：

```
wsl --list --online
```

安装一个发行版。例如安装Ubuntu：

```
wsl --install -d Ubuntu
```

如果在Powershell中使用Linux命令，需要先输入wsl ，然后开始写Linux命令。 
	
